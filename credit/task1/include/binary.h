#ifndef BINARY_TO_DECIMAL_H
#define BINARY_TO_DECIMAL_H

/* Convert string binary number to integer
   Sets error to 1 on invalid binary number */
unsigned int binaryToInteger(char* binaryNumber, int* error);

#endif
